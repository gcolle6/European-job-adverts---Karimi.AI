# Week 1 — audit, atomisation, lexicon classification
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet output for the requirement table

# Week 1 days 5-7 — multilingual embedding and the alignment check
sentence-transformers>=3.0
torch>=2.0             # CPU build is sufficient for this corpus
scikit-learn>=1.5

# Week 2 — clustering (not yet installed)
# umap-learn>=0.5
# hdbscan>=0.8
